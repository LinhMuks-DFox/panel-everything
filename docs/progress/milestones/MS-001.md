---
id: MS-001
title: "基础设施跑通"
status: planned
target_date:
delivered_date:
---

## 目标

搭建整个项目的运行底座：单进程 FastAPI 应用在 Docker 容器内启动，采集框架可注册空 Collector，SQLite(WAL) 数据库正确初始化，前端空白面板在三类终端（Kindle/iPhone/iPad）上响应式渲染，健康检查端点返回 200。

本里程碑完成后，后续所有模块（ARCH-002/003/004）可并行在此底座上叠加，不再需要修改基础设施。

## 包含需求

| REQ ID | 标题 | 状态 |
|--------|------|------|
| REQ-001 | 系统整体约束与部署要求 | draft |

## 关联任务

| TASK ID | 标题 | 优先级 | 状态 |
|---------|------|--------|------|
| TASK-001 | 项目骨架 + Dockerfile(多阶段多 arch) + compose + /healthz | P0 | todo |
| TASK-002 | SQLite(WAL) 连接 + schema 基线 + repository 薄层 | P0 | todo |
| TASK-003 | Collector 框架:协议 + 注册表 + 调度器 + 框架级降级 | P0 | todo |
| TASK-004 | SSR 前端壳:base 布局 + 响应式/e-ink CSS + 轮询/meta-refresh 降级 | P0 | todo |
| TASK-005 | 配置与凭证管理 + response model 白名单 + 日志脱敏 | P0 | todo |

## 交付检查

- [~] 所有关联 TASK 状态为 done（TASK-001~005 当前 review，待 Architect 复核置 done）
- [ ] `docker compose up` 在 arm64（树莓派）和 amd64 均可成功启动（amd64 build 已验，arm64 待硬件）
- [x] `GET /healthz` 返回 HTTP 200，响应体含 `{"status": "ok"}`（冒烟验证 db 亦为 ok）
- [ ] 容器内存占用空闲时不超过 150 MB（无任何 Collector 运行时）（待容器实测）
- [x] 空白面板页面在 Kindle 浏览器上可正确加载（`<meta refresh 60>` 降级有效，Kindle UA curl 验证含 meta refresh 且不加载 panel.js）
- [~] 空白面板页面在 iPhone Safari（竖屏）上响应式布局正确（SSR 200 + 栅格容器，真机目检待人类）
- [~] 空白面板页面在 iPad Safari（横/竖屏）上响应式布局正确（同上，待人类目检）
- [x] 注册一个空 Collector（只返回 `[]`），调度器正常触发并写 `collector_run` 记录（test_collectors 覆盖：NullCollector → run_collector → collector_run.status=up）
- [x] SQLite WAL 模式开启，`PRAGMA journal_mode` 返回 `wal`（冒烟 DB 实测 wal）
- [x] `ruff check src/` 零报错
- [x] 测试全部通过（`pytest tests/` 绿色，69 passed）
- [x] 无 critical/major bug（集成冒烟未发现；修复两处 minor 配置未装配问题）
- [ ] 人类验收通过

> 冒烟说明（2026-06-28，claude-opus-4-8[1m]）：在仓库根 `.venv` 跑全套 69 测试通过；真实 uvicorn 起 `panel.main:app` 验证 healthz/首页/静态资源/三端渲染骨架/WAL/建表。集成修复见 changelog [MS-001]。代码层 MS-001 已达成；剩余未勾项为容器实测（arm64/内存）与真机目检/人类验收，需对应环境完成。

## 测试覆盖

- TASK-001：Dockerfile 多阶段构建成功，compose healthcheck 通过
- TASK-002：通用三表建表 idempotent（IF NOT EXISTS），upsert_snapshot / append_history / record_collector_run 单测
- TASK-003：MetricSample dataclass 字段校验；registry 注册/查询；scheduler 触发后写 collector_run；框架级 timeout 降级产出 `status="error"` MetricSample
- TASK-004：Jinja2 模板渲染无异常；三断点 CSS 人工目检；无 JS 降级时页面仍可读
- TASK-005：pydantic-settings 从 env 正确加载；白名单 response model 不含敏感字段；日志不含凭证关键字

## 遗留问题

- HTTPS 由 Tailscale Serve/Funnel 处理，本里程碑不单独实现（后期 P3）
- SSH host key 强校验（known_hosts 验证）列为 P3，本期 `known_hosts=None`，内网假设下可接受
- 历史数据保留策略（retention 裁剪 job）暂未实现，待数据量实际增长后评估
