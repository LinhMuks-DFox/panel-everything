# 2026-06

### [MS-002] 整合验证与修复（Azure VM + GPU + Tailscale 同屏交付）
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 全套 `pytest`（含 integration）**309 passed, 0 failed**；`-m "not integration"` 为 308 passed 1 deselected。修复后 `test_live_smoke`（活体 Tailscale）首次转绿，解析出 9 个真实 tailnet 节点（muxrpi=ONLINE 等）。新增 BUG-001 记录。
- **备注**: 执行模型 claude-opus-4-8[1m]（Opus 4.8, 1M context）。
  - **整合修复（最小修复，不加新功能）**：发现并修复 BUG-001（major）——`src/panel/collectors/tailscale/collector.py` 的 localapi 调用无法连通现代 tailscaled（1.50+）。根因两点：(1) Host 需为 `local-tailscaled.sock`（带 .sock 后缀），旧值 `http://local-tailscaled` 被判 `invalid localapi request`；(2) 需带 `Sec-Tailscale: localapi` 请求头（localapi CSRF 防护），旧代码不带。修复：引入 `LOCALAPI_HOST = "local-tailscaled.sock"` / `LOCALAPI_BASE` 与 `LOCALAPI_HEADERS`，`_fetch_status()` 传 `headers=LOCALAPI_HEADERS`。curl 双重验证（Host .sock + Sec-Tailscale 头 → 200）。详见 `docs/progress/bugs/BUG-001.md`。
  - **装配完整性核验（无遗漏，无需改动）**：`main.register_collectors` 已集中调用 azure/gpu/tailscale 三个 `register()` 工厂（缺配置走 warning+skip 不报错）；`create_app` 已 `include_router(azure_router)`（/api/v1/servers）与 `include_router(tailscale_router)`（/api/tailscale）；`index.html` 已 include `_datasource_status` / `_vm_card`（Azure VM + GPU）/ `_node_grid`（Tailscale）三区块；建表齐全（`schema.sql` 含 latest_snapshot/metric_history/collector_run/servers/azure_vm_status/gpu_metrics(+5m/+1h)，`migrations/003_tailscale.sql` 含 tailscale_nodes/tailscale_node_events），`migrate.run` 启动幂等执行 schema + 全部 migrations。
  - **活体端到端验证**：`uvicorn panel.main:app` 启动于 8080，启动日志显示 Azure disabled（未配置，优雅）+ GPU registered + Tailscale registered。`/healthz`=200 `{"status":"ok","db":"ok"}`；`/`=200 且单屏同含 `data-module="azure"`（Azure VM + GPU，无配置时标「未配置」不崩）与 `data-module="tailscale"`；`/api/v1/servers`=200 `[]`；`/api/tailscale/nodes|status`=200。`POST /api/tailscale/refresh` 触发真实采集，9 节点入库，`/api/tailscale/nodes` 与 `tailscale status` 节点数一致（muxrpi/takamichi-lab-pc15 等 ONLINE），响应体不含 `node_key`（白名单生效）。页面真实数据渲染出 status-ok/warn/error 三态徽标。
  - **单源故障隔离验证**：脚本注入一个 `collect()` 抛异常（含 secret token）的 collector，`run_collector` 不外泄异常、降级 `status=error`、token 经 scrub 打码为 `***`、collector_run 落库；其余 collector 与 SSR 渲染不受影响（routes.py 每个模块数据加载各自 try/except 兜底）。
  - **MS-002 验收**：达成。Azure VM 状态 + GPU 利用率/显存 + Tailscale 在线态同屏呈现；单机不可达/数据源异常不塌全局；数据持久化 SQLite（WAL）。遗留：Azure 真实 SP 凭证待部署后配置（本机无凭证，走 mock+单测路径，符合 MS-002 计划）。

### [TASK-022] 前端 NodeCard/NodeGrid/StaleWarning（e-ink 适配）
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/web/test_tailscale_render.py` 20 个用例，全部通过；全套回归 308 passed 1 deselected（integration skip）0 non-integration failed（288→308）；`ruff check src tests` 零 error。覆盖：首页含 data-module="tailscale" section；ONLINE→status-ok+●、OFFLINE→status-warn+◐、LONG_OFFLINE→status-error+○；三态同时存在时三个符号均出现；StaleWarning 横幅在 is_stale=True 时出现（"数据已超过"/"未更新"文本）、is_stale=False 时不出现；collector_status=error 时渲染错误横幅、status=up 时不出现；nodes=[] 时 HTTP 200 且 node-grid 容器存在但无 node-card；node-summary "2/3 在线"计数正确；index.html 含 `_node_grid.html` include；partials/_node_grid.html 和 _node_card.html 文件均存在；ARCH-003 CSS section 存在且无 box-shadow / 无 animation/@keyframes；datetimeformat 过滤器：None→"—"、aware datetime→"YYYY-MM-DD HH:MM UTC"、naive datetime 作 UTC 处理。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/web/templates/partials/_node_card.html`（新建）：单节点卡片 partial。三态徽标（color+shape+aria-label：ONLINE ● status-ok、OFFLINE ◐ status-warn、LONG_OFFLINE ○ status-error）；IP/OS/最后在线 dl；exit node 徽标 ⇢；stale mark ◌。e-ink 安全：无 box-shadow，无 animation，无 transition。
  - `src/panel/web/templates/partials/_node_grid.html`（新建）：节点网格 + StaleWarning + 采集器状态横幅。context 变量：nodes/nodes_online/nodes_total/collector_status/collector_error/is_stale/stale_seconds。循环 include _node_card.html。is_stale=True 时显示 stale 横幅；collector_status not in (up, never_run) 时显示 datasource-banner--issues 错误横幅。
  - `src/panel/web/templates/index.html`（修改）：在 _vm_card.html include 之后追加 `{% include "partials/_node_grid.html" %}`，将 Tailscale section 接入主布局。
  - `src/panel/web/static/css/panel.css`（修改）：追加 ARCH-003 Tailscale 节点网格样式（.node-grid / .node-card / .node-card__header / .node-card__hostname / .node-card__meta / .node-card__badge--exit / .node-card__stale-mark / .node-summary）；三断点响应式（<600px minmax(140px)、600-1023px minmax(160px)、≥1024px minmax(180px)）；LONG_OFFLINE opacity:0.55 视觉降调；print/e-ink 覆盖规则；无 box-shadow/animation/@keyframes。
  - `src/panel/web/static/js/panel.js`（修改）：在文件中段追加独立 IIFE Tailscale 节点轮询模块（45s 间隔，独立 timer，Page Visibility 与 ARCH-001 外层轮询互不干扰）。renderNodeGrid() 仅更新 data-state/status-dot class+symbol/stale mark，不重建 DOM，避免闪烁。
  - `src/panel/web/routes.py`（修改）：(1) 注册 `_datetimeformat` Jinja2 过滤器（None→"—"，aware/naive datetime→UTC 格式化字符串）。(2) index() 路由扩展：读 repo.get_all_nodes()/get_last_run("tailscale")，计算 is_stale（ran_at 与 stale_threshold 比较）；为每个节点构造 SimpleNamespace view-model（slots=True dataclass 不可直接加属性，用 SimpleNamespace 桥接，添加 is_stale 字段和 last_seen 重命名）；注入 nodes/nodes_online/nodes_total/collector_status/collector_error/is_stale/stale_seconds 到模板上下文；repo 不可用时全部降级为空值，不报错。
  - 关键决策：(1) TailscaleNodeRow 使用 slots=True dataclass，不能动态加属性；用 SimpleNamespace view-model 桥接，避免修改 dataclass 定义。(2) is_stale 基于每节点 collected_at 而非全局 last run 时间，使每张卡片的陈旧状态独立（与 TASK-021 API 逻辑一致）。(3) 全局 is_stale（StaleWarning 横幅）基于 collector_run 的 ran_at，与单节点 is_stale 分离，两者并存不冲突。(4) CSS comment 中避免出现 `@keyframes` 字面量（改写为"keyframe blocks"），确保测试 regex 不误报。(5) Tailscale JS 轮询与 Azure 轮询、ARCH-001 全页刷新三者共存于 panel.js，各自独立 IIFE + 独立 timer，互不干扰。

### [TASK-021] Tailscale REST API
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_tailscale_api.py` 22 个用例，全部通过；全套回归 288 passed 0 failed（266→288）；`ruff check` 零 error。覆盖：GET /nodes 空表返回 []；GET /nodes 含全部节点，is_stale 字段在每条均存在且为 bool；is_stale=False（30s < 90s 阈值）/ is_stale=True（120s > 90s）；?stale=true 只返回 stale 节点（2 中 1）；node_key 不出现在 GET /nodes 和 GET /nodes/{id} 任何响应体中；GET /nodes/1 返回正确节点；GET /nodes/9999 → 404；GET /status 无记录时 never_run，有 up 记录时 status=up+sample_count+ran_at，有 error 时 error 字段非空；POST /refresh job 存在时 triggered=True + scheduler.modify_job 被调用；POST /refresh job 不存在时 triggered=False（JobLookupError，不抛 500）；ASGI 全链路 round-trip 验证 /nodes、/nodes?stale=true、/nodes/9999、/status、/refresh 各自预期状态码。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/api/tailscale/__init__.py`（新建）：空包初始化文件。
  - `src/panel/api/tailscale/routes.py`（新建）：`APIRouter(prefix="/api/tailscale")`，四个端点完整实现。`_stale_threshold(request)` 从 `app.state.settings.tailscale_stale_threshold_seconds` 读阈值（不存在则回落 90s 默认值），无需循环依赖。`list_nodes` 逐行计算 `is_stale`，`stale=True` 参数时过滤仅返回 stale 节点。`get_node` 404 逻辑用 `HTTPException(status_code=404)`。`get_collector_status` 调 `repo.get_last_run("tailscale")`，无记录返回 `status="never_run"`。`manual_refresh` 用宽 `except Exception` 捕获所有调度器异常（含 `JobLookupError`）返回 200+triggered=False，绝不抛 5xx。
  - `src/panel/api/deps.py`（修改）：追加 `get_scheduler(request)` 依赖注入函数，从 `app.state.scheduler` 读取 `AsyncIOScheduler`。
  - `src/panel/db/repository.py`（修改）：在 `get_all_last_runs` 之前追加 `get_last_run(collector: str) -> CollectorRunRow | None` 方法——按 `collector` + `id DESC` 取最新一行（任意 status），供单模块 `/status` 端点使用。
  - `src/panel/domain/models.py`（修改）：追加 `from typing import Literal` 导入；追加三个 Pydantic 响应模型：`NodeResponse`（继承 PublicModel，白名单不含 node_key）、`CollectorStatusResponse`、`RefreshResponse`。
  - `src/panel/main.py`（修改）：导入并注册 `tailscale_router`（`app.include_router(tailscale_router)`），集中挂载于 `create_app`。
  - 关键决策：(1) stale 阈值从 `request.app.state.settings` 懒读，避免在模块加载时调 `get_settings()`（单例缓存会被测试改写）。(2) `manual_refresh` 用宽 `except Exception` 而非 `except JobLookupError`，因 APScheduler 版本差异可能抛不同异常类型，宽捕获更健壮，日志保留原始 `type(exc).__name__`。(3) `get_last_run` 按 `id DESC` 而非 `ran_at DESC` 排序，避免时钟回拨或同秒多次采集时顺序不确定。(4) 测试中 `_coro(value)` helper 直接返回 coroutine，与 MagicMock 的 `return_value` 配合，无需 `AsyncMock`（避免 Python 3.11 以下兼容性差异）。
  - 给后续 Coder（TASK-022 前端 NodeCard/NodeGrid）：`GET /api/tailscale/nodes` 返回 `list[NodeResponse]`，`is_stale` 字段用于 StaleWarning 横幅判断；`GET /api/tailscale/status` 返回 `CollectorStatusResponse`，前端可用 `status` 字段渲染数据源状态条；`POST /api/tailscale/refresh` 触发手动刷新，`triggered` 字段指示是否成功调度；端点已在 `main.create_app` 中注册，无需额外操作。

### [TASK-020] Tailscale 采集器 + 数据库表 + 在线判定逻辑
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/collectors/tailscale/test_collector.py` 17 个用例 + `tests/collectors/tailscale/test_online_state.py` 16 个用例，共 33 通过；全套回归 266 passed 0 failed（233→266）；`ruff check` 零 error。覆盖：fixture 驱动 9 节点解析（Self+8 Peer）/ muxrpi=ONLINE / ipad163=LONG_OFFLINE / iphone-13=OFFLINE / 5 在线节点全 ONLINE；upsert 首次插入写 first_seen 事件 / 状态不变不写事件 / ONLINE→OFFLINE 写变更事件；socket 不可达抛 ClientConnectorError / 经 run_collector 框架降级为 status='error'；register() socket 不存在时 warning+不注册 / socket 存在时注册成功；determine_online_state 全三态边界参数化 8 组（online=True/False × last_seen=None/23h55m/24h/24h1m/48h 等）+ 自定义阈值覆盖。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/db/migrations/003_tailscale.sql`（新建）：`tailscale_nodes`（node_key UNIQUE、online_state、is_exit_node、last_seen_at 等）+ `tailscale_node_events`（event-driven，仅状态变更 INSERT）+ 两个索引，全 IF NOT EXISTS 幂等。
  - `src/panel/db/migrate.py`（修改）：扩展 `run()` 以按文件名升序加载 `db/migrations/*.sql`，向后兼容（目录不存在时返回空列表）。
  - `src/panel/db/repository.py`（修改）：追加 `TailscaleNodeRow` / `TailscaleNodeEventRow` dataclass；以模块尾部注入方式为 `Repository` 追加 `upsert_tailscale_node`（UPSERT+event-driven 状态事件）/ `get_all_nodes` / `get_node_by_id` / `get_node_events` 四个方法。
  - `src/panel/collectors/tailscale/collector.py`（新建）：`TailscaleCollector`（Collector 协议）；`determine_online_state()` 纯函数（ONLINE/OFFLINE/LONG_OFFLINE 三态，long_offline_hours 可配）；`collect()` 通过 `aiohttp.UnixConnector` 调 localapi，并发 `asyncio.gather` upsert 全节点，socket 不可达向上抛让框架降级，单节点失败隔离为 status='unreachable'。
  - `src/panel/collectors/tailscale/__init__.py`（新建）：`register(settings, repo)` 工厂，socket 不存在则 warning 跳过。
  - `src/panel/collectors/__init__.py`（修改）：在 `register_collectors` 中追加 `register_tailscale(settings, repo)` 调用。
  - `tests/collectors/tailscale/fixtures/localapi_status.json`（新建）：9 节点录制 fixture（muxrpi/muxdesktop-wsl-ubuntu/takamichi-lab-pc15/mux-mbp/muxdesktop-windows 在线；ipad163 LONG_OFFLINE；iphone-13/iphone181/trl-pc OFFLINE）。
  - 关键决策：(1) migrate.py 扩展为"基线 schema.sql + migrations/ 升序"两段执行，而非单文件追加，便于模块独立维护 DDL，不触碰其他模块的文件。(2) Repository 扩展用模块尾部注入（setattr），避免修改类体 + import 循环风险；ruff UP037 已修复（去字符串引号）。(3) collect() 内部也主动调用 upsert_snapshot，确保 latest_snapshot 通用表立即可查；框架层随后的写入是幂等重复，不影响正确性。(4) live smoke test 以 pytest.mark.integration 标记；WSL 环境下 tailscale localapi 需 root 权限（SO_PEERCRED 验证），本机 uid=1000 收到 403，属预期——树莓派容器以 root 运行时正常，不影响非 integration 测试全绿。(5) pyproject.toml 新增 markers 注册 integration mark，消除 PytestUnknownMarkWarning。

### [TASK-015] 前端 VmCard + GpuCard + 状态徽标（e-ink 适配）
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_frontend_vm_gpu.py` 44 个用例，全 passed 0 failed；全套回归 233 passed 0 failed（189→233）；`ruff check src tests` 全绿。覆盖：`vm_status_class`（Running/Stopped/Deallocated/Starting/Stopping/Deallocating/Unknown + is_stale 各分支）；`vm_status_symbol`（ok●/warn◐/stale◌/error○）；`util_threshold_class` 7 个参数化边界值；`mem_threshold_class`（None→"" + 5 个参数化边界值）；`GET /` 含 `data-module="azure"` section（有 repo）/ 无 repo 优雅降级；渲染 VM 名称+power_state / 资源组 / GPU card 含 util+mem bar / GPU util_pct=None 渲染"不可达" / is_stale=True 渲染⚠陈旧标签 / vms=[] 渲染空态提示；HTML 无 ssh_key_path；panel.css ARCH-002 section 存在 / 无 box-shadow / 无 animation/@keyframes / 含 bar-ok/warn/critical；index.html 含 include _vm_card.html；_vm_card.html 存在且含 status-dot 和 metric-bar。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/web/templates/partials/_vm_card.html`（新建）：VmCard + GpuCard 嵌套 partial。VmCard 含状态徽标（三重编码：color+shape●◐○◌+text）、VM 名称、power_state 标签、stale-badge（⚠ 陈旧）、资源组 dl。GpuCard 含算力/显存 metric-bar（`metric-bar__fill bar-fill bar-*` 双 class 复用 ARCH-001 BEM + 新增 threshold 颜色）、temp/power meta row、util_pct=None 时渲染"不可达"区块。e-ink 安全：无 box-shadow，无 transition，无 animation。
  - `src/panel/web/routes.py`（修改）：(1) 新增四个 Jinja2 全局函数 `_vm_status_class` / `_vm_status_symbol` / `_util_threshold_class` / `_mem_threshold_class`，注册到 `templates.env.globals`，供模板直接调用。(2) `index()` 路由扩展：加载 `app.state.gpu_repo`，条件调用 `build_azure_dashboard(repo, gpu_repo)` 并将结果注入模板上下文 `azure_dashboard`；repo/gpu_repo 不可用时 `azure_dashboard=None`，不报错。
  - `src/panel/api/azure.py`（修改）：将 dashboard 聚合逻辑从 FastAPI handler 中提取为独立异步函数 `build_azure_dashboard(repo, gpu_repo) -> DashboardAzureOut`，handler 仅委托调用。SSR 路由通过直接 `await build_azure_dashboard(...)` 复用，避免内部 HTTP 往返，保持单进程低开销。
  - `src/panel/web/templates/index.html`（修改）：追加 `{% include "partials/_vm_card.html" %}`；调整占位符条件（仅在 azure_dashboard 也为 None 时显示）。
  - `src/panel/web/static/css/panel.css`（修改）：追加 `ARCH-002: Azure VM / GPU Cards` 块。新增 `.vm-card` / `.vm-header` / `.vm-name` / `.vm-state-label` / `.stale-badge` / `.vm-meta` / `.metric-bar-row` / `.metric-label` / `.metric-value` / `.bar-fill.bar-ok/warn/critical` / `.gpu-list` / `.gpu-card` / `.gpu-card.gpu-stale` / `.gpu-label` / `.gpu-name-small` / `.gpu-meta-row` / `.gpu-unreachable`。`@media print`（e-ink/截图）降级：边框纯黑、无 border-radius、bar 颜色灰化。`@media screen (max-width:800px) and (color-index:2)`（Kindle）降级：vm-card/gpu-card 无 border-radius，gpu-card 无 background。**全段零 box-shadow、零 animation、零 @keyframes。**
  - `src/panel/web/static/js/panel.js`（修改）：追加独立 IIFE。`refreshAzure()` fetch `/api/v1/dashboard/azure`（JSON），`renderAzureDashboard(data)` 按 server_id diff 就地更新现有 `.vm-card`，对新 VM append，对删除 VM remove——比全页 innerHTML 替换粒度更细、闪烁更少。`startAzurePolling()` / `stopAzurePolling()` 45 s interval；`visibilitychange` 钩子 tab 切回立即 `refreshAzure()` + restart。
  - 关键决策：(1) 继承 ARCH-001 `metric-bar__fill` BEM class，追加 `bar-fill` 作为"带阈值颜色的 fill"标记，`bar-ok/warn/critical` 只生效于 `.bar-fill.*`，避免改动既有 `.metric-bar__fill.status-*` 规则。(2) `build_azure_dashboard` 提取为 public 函数（非私有方法）供 SSR 路由 import，不改变 API 端点行为，TASK-014 的 18 个测试零改动全通。(3) JS 渲染选择 diff 策略（按 server_id 匹配就地替换）而非整块 innerHTML，保留已展开的 GPU list 位置，轻量且无闪。(4) Jinja2 全局函数使用 Python match/case（3.10+）语法，与代码库 Python 3.12 基线一致。

### [TASK-014] Azure + GPU dashboard 聚合 API
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_dashboard_azure.py` 18 个用例，全 passed 0 failed；全套回归 189 passed 0 failed（171→189）；`ruff check src tests` 全绿。覆盖：GET /api/v1/dashboard/azure 返回 200 / 响应体三顶层 key / servers 空时 vms=[] / collector_status 始终含 azure_vm+gpu 两 key / fetched_at 带 tz 信息；collector_status 无记录→unknown / 有 up 记录→up+last_ran_at 非空 / 有 down 记录→down+error 不含路径密钥 / 多条取最新；VM 从未采集→power_state="Unknown"+is_stale=True / 采集时间在 600s 内→is_stale=False / 超 600s→is_stale=True；has_gpu=False→gpus=[] / GPU 采集在 180s 内→is_stale=False / 超 180s→is_stale=True / 多轮采集只返回最新 / DashboardVmOut 字段完整校验 / 响应体不含 ssh_key_path。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/api/azure.py`（修改）：在既有 `/api/v1/servers` CRUD router 上追加 `GET /api/v1/dashboard/azure`（`response_model=DashboardAzureOut`）。新增模块级私有函数 `_parse_utc`（ISO8601→tz-aware datetime）、`_build_collector_status`（list[CollectorRunRow]+names → dict[str, CollectorStatusOut]，未知 collector 映射 status="unknown"）、`_build_gpu_outs`（list[GpuMetricRow]+now → list[GpuMetricOut]，计算 GPU is_stale）。stale 阈值常量 `VM_STALE_SECONDS=600` / `GPU_STALE_SECONDS=180` 置模块顶部。端点同时依赖 `get_repo`（读 collector_run）和 `get_gpu_repo`（读 servers/azure_vm_status/gpu_metrics），两路 Depends 均已存在于 deps.py。
  - 关键决策：(1) `get_latest_gpu_metrics` 和 `get_vm_status_all` 在 TASK-010/013 中已实现，本卡零 repository 改动。(2) VM 从未采集时 `collected_at=now`（placeholder），而非写入 None，保持 Pydantic model 类型为 `datetime`（非 Optional）。(3) has_gpu=False 的服务器完全跳过 `get_latest_gpu_metrics` 查询，减少无效 SQL；has_gpu=True 但 gpu_metrics 无记录时返回 `gpus=[]`（正常空态）。(4) `_build_collector_status` 按 names 参数遍历而非遍历 last_runs，确保即使某 collector 从未运行也会有 status="unknown" 条目。(5) `collector_run.error` 字段已由 scheduler 层（ARCH-001）写库前脱敏，API 层直接透传，无需二次处理。
  - 给后续 Coder：TASK-015（前端 VmCard/GpuCard）可直接调用本端点填充卡片数据；TASK-016（降采样 job）不影响本端点（端点读 gpu_metrics 实时表，降采样表供趋势 API 单独查询）。

### [TASK-013] SSH GPU 采集器（asyncssh + nvidia-smi 多卡解析）
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_gpu_collector.py` 26 个用例，全 passed 0 failed；全套回归 171 passed 0 failed（145→171）；`ruff check src tests` 全绿。覆盖：CSV 解析（单卡字段精确值 / 多卡 4 卡 index 0~3 / 空输出零行 / [Not Supported] 与 [N/A] 占位置 None 但整行仍 ok / 字段数不符标 error / 非数字 index 标 error / 跳空行）；状态分类（exit≠0→error / 输出空→error / asyncssh.DisconnectError→unreachable / OSError→unreachable / asyncio 超时→unreachable）；多台并发隔离（ok+unreachable+error 三机互不影响，ok 机卡数据照常落库；_collect_one 内未预期异常经 gather 兜底降级为 error）；无 has_gpu 机返回空列表且不连任何主机；非 GPU 机被跳过只 SSH GPU 机；写库 append_gpu_metrics 被以正确 GpuSample（4 卡、mem_pct 由 repository 计算）调用；汇总 MetricSample（gpu_any_running / value_num / value_text "{ok}/{total} gpus ok" / status）；Collector 协议 isinstance + 默认 name/interval/timeout；register() 始终注册且无服务器时 collect() 空；AsyncSshRunner 默认执行层 monkeypatch asyncssh.connect 验证连接失败透出异常 + 成功路径返回 stdout/exit_status；ssh_key_path 不出现在任何采集产出中。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - `src/panel/collectors/gpu/collector.py`（新建）：`GpuCollector`（dataclass，满足 ARCH-001 Collector 协议，name="gpu" interval=60s timeout=30s）。`collect()` 读 servers 过滤 has_gpu=True → `asyncio.gather(return_exceptions=True)` 并发各机 `_collect_one` → 汇总所有卡 GpuSample 一次性 `append_gpu_metrics` 写专用表 → 每台机产出一条 `MetricSample(metric="gpu_any_running")` 交框架写通用表。`_collect_one` 用 `asyncio.timeout(timeout_seconds)` 包 SSH 操作，捕获 `(TimeoutError, asyncssh.Error, OSError)`→unreachable、exit_status≠0 / 无可识别行→error。`_parse_nvidia_smi_csv` 纯函数解析（字段数校验、`_to_float` 安全转换、`[Not Supported]`/`[N/A]` 占位识别）。
  - SSH 执行层抽象 `SshRunner` 协议 + `AsyncSshRunner`（基于 asyncssh，`known_hosts=None` 处含内网假设注释 + P3 强校验 TODO）经实例属性 `ssh_runner` 注入——这是与 TASK 卡「SSH 执行层可注入」要求的对齐落地，单测注入 FakeSshRunner 完全不连真实主机。
  - `src/panel/collectors/gpu/__init__.py`（新建）：`register(settings, repo, gpu_repo)` 工厂，始终注册（GPU 无凭证开关，SSH 私钥按路径存 servers 表）。
  - `src/panel/collectors/__init__.py`（修改）：`register_collectors` 内追加 `register_gpu(settings, repo, gpu_repo)`。
  - `tests/fixtures/gpu/`（新建）：nvidia_smi_single / multi / empty / partial 四个 CSV 样例。
  - `tests/test_servers_api.py`（修改）：补 autouse `_clean_registry` fixture。原因：该文件每个用例都进入一次 app lifespan（其内 register_collectors 注册始终启用的 gpu collector），注册表是进程级全局字典，不复位会触发「collector already registered」——这是 TASK-013 让 gpu 变为「始终注册」后暴露的既有测试卫生缺口（此前 azure 因缺凭证 disabled 故无碰撞），沿用 test_collectors/test_azure_collector 既有清理约定修复，未改生产代码。
  - 关键决策：(1) SSH 执行层做成可注入 Protocol（SshRunner），把 asyncssh 细节隔离在 AsyncSshRunner，单测主路径全部走 FakeSshRunner，仅两个用例 monkeypatch asyncssh.connect 验证默认实现——既满足「不实际 SSH」又对默认执行层留有覆盖。(2) GpuSample 写专用表在 collect() 内完成，返回的是每台机的汇总 MetricSample（交框架写通用 latest_snapshot/metric_history + 统计 sample_count），与 ARCH-002「双写」语义一致。(3) 汇总态：有任一 ok 卡→ok，否则 unreachable 优先于 error（不可达比解析错更能代表整机失败主因）。(4) `mem_pct` 不在 GpuSample 计算，由 `GpuRepository.append_gpu_metrics` 写库时算（与 TASK-010 约定一致）。(5) ASYNC109：执行层 run() 的超时参数命名为 `timeout_seconds` 而非 `timeout`，规避 ruff ASYNC109 且语义更准。(6) MS-003/TASK-016 的 deallocated 跳采优化按 TASK 卡要求在 `_collect_one` 开头以注释预留 `_is_vm_running` 接口位，本期不实现。
  - 给后续 Coder：TASK-014（dashboard 聚合）`collector_status` 中 GPU 的 key 用 `"gpu"`（= collector.name），读 `collector_run` 最近一次运行判 up/down/error；各机 GPU 列表读 `GpuRepository.get_latest_gpu_metrics(server_id)`，stale 用 GpuMetricRow.collected_at 与 stale_threshold（GPU 默认 180s）比较。TASK-016（降采样）从 `gpu_metrics` 聚合到 `gpu_metrics_5m/1h`，并可在 `_collect_one` 落实 deallocated 跳采（取消预留注释）。

### [TASK-012] Azure VM 采集器（ClientSecretCredential, Reader）
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_azure_collector.py` 29 个用例，全 passed 0 failed；全套回归 145 passed 0 failed（116→145）；`ruff check` 全绿。覆盖：六种电源态映射 + 大小写不敏感 + Unknown（未知 code / 缺 PowerState / 空 / None）；fixture 驱动 running / mixed / empty / no_powerstate；dict 与 SDK 属性对象两种形态均可解析；只采集已注册（azure_vm_name 匹配）的 VM、未注册跳过、无注册机返回空、azure_vm_name=None 不参与匹配；单台解析失败隔离（产出 status='error' 不污染其它台）、失败不 upsert（旧值保留）；整体 SDK 异常向上传播 + 经 run_collector 框架降级为 collector_run.status='error'；Collector 协议 isinstance + 默认 name/interval/timeout；register() 工厂——未配置/部分配置/secret 文件不存在均优雅跳过不抛异常、凭证齐全时打桩 SDK 注册成功、注册过程不泄露 secret 到日志。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - `src/panel/collectors/azure/collector.py`（新建）：`AzureVmCollector`（dataclass，满足 ARCH-001 Collector 协议，name="azure_vm" interval=300s timeout=60s）。`collect()` 先读 servers 表按 azure_vm_name 建索引（只监控显式注册的机器，避免越权采集），`asyncio.to_thread` 包同步 SDK `list_all(expand="instanceView")`，逐台解析电源态 → upsert azure_vm_status → 产出 `MetricSample(metric="power_state", value_text=展示态, value_num=is_running)`。`_parse_power_state` 取 code 前缀 `PowerState/`（大小写不敏感）映射；`_get` 统一兼容 SDK 属性对象与 dict。
  - `src/panel/collectors/azure/__init__.py`（新建）：`register(settings, repo, gpu_repo)` 工厂；四项 Azure 配置任一缺失或 secret 文件读不到 → warning + return（collector disabled），不抛异常；齐全则 `read_secret()` 读 secret 文件 → `ClientSecretCredential` → `ComputeManagementClient`（单例注入）→ registry.register。azure SDK 延迟导入（仅启用时才 import）。
  - `src/panel/config/settings.py`（修改）：新增 `azure_subscription_id` 字段 + `azure_configured` property（四项齐全才 True）。**遵循 ARCH-001 凭证按路径约定**，保留既有 `azure_client_secret_file`（路径），secret 内容经 `read_secret()` 懒加载，不入 env 明文、不入 DB——与 TASK 卡示意的 `SecretStr` 等价但更符合基线（凭证只存路径引用）。
  - `src/panel/collectors/__init__.py`（修改）：`register_collectors` 签名追加 `gpu_repo` 参数，函数体调用 `register_azure(settings, repo, gpu_repo)`（azure 工厂内部按配置自行启停）。
  - `src/panel/main.py`（修改）：lifespan 调用处改为 `register_collectors(settings, app.state.repo, app.state.gpu_repo)`。
  - `tests/fixtures/azure/`（新建）：list_vms_running / mixed / empty / no_powerstate 四个录制响应 JSON。
  - 关键决策：(1) 凭证按路径约定优先于 TASK 卡的 SecretStr 写法——ARCH-001/ARCH-002 均明确 secret 不入 env/DB 明文，故沿用 `azure_client_secret_file` + `read_secret()`，对外行为一致。(2) `collect()` 内单台 VM 失败走 try/except 隔离并产出 error sample、不 upsert（保留旧值供前端 is_stale 提示陈旧），整体 SDK 失败则 raise 交框架层降级——两层降级语义与 ARCH-001 一致。(3) 只采集 servers 表已注册且 azure_vm_name 匹配的 VM，未注册的 Azure VM 一律跳过。(4) azure SDK 延迟导入，缺凭证环境零导入开销。
  - 给后续 Coder：TASK-013（GpuCollector）同样在 `register_collectors` 内追加 `register_gpu(...)`；TASK-014（dashboard 聚合）`collector_status` 中 azure 的 key 用 `"azure_vm"`（= collector.name），读 `collector_run` 最近一次运行判 up/down/error，VM 列表读 `azure_vm_status` JOIN `servers`，stale 用 collected_at 与 stale_threshold 比较（VM 默认 600s）。

### [TASK-011] 服务器注册 CRUD API（凭证不回传）
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_servers_api.py` 20 个用例，全 passed 0 failed；全套回归 116 passed 0 failed；`ruff check` 全绿。覆盖：POST 成功 201 / 响应字段集合与 ServerOut 精确一致 / 响应体不含 ssh_key_path / 响应值校验 / 重复 name 409 / 最小 payload 成功 / 缺少 name 422；GET 空库返回空数组 / 注册后列表出现 / 列表无 ssh_key_path / 多台有序返回 / 列表字段集合一致；DELETE 存在 id 204 / 不存在 id 404 / DELETE 后 GET 消失 / 只删目标不影响其他 / 两次 DELETE 第二次 404；DB 直接 SQL 验证 ssh_key_path 确实写入；/healthz 与 /api/v1/servers 同进程可访问。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/api/deps.py`（新建）：`get_repo` 和 `get_gpu_repo` 两个依赖注入函数，从 `request.app.state` 读取对应 Repository 实例，与 ARCH-001 deps.py 模式对齐。
  - `src/panel/api/azure.py`（新建）：`APIRouter(prefix="/api/v1", tags=["servers"])`，三个端点 POST/GET/DELETE `/servers`；`_row_to_out` 显式字段映射（不用 `**dict`），彻底隔离 ssh_key_path；POST 捕获 `aiosqlite.IntegrityError` 转 409（raise from exc），通用异常转 500（仅记录异常类型，不记录路径内容）；DELETE 根据 `delete_server()` 布尔返回值决定 204 或 404。
  - `src/panel/main.py`（修改）：lifespan 中追加 `app.state.gpu_repo = GpuRepository(conn)`；create_app 中 `app.include_router(azure_router)`；同时修复 `app.state.settings` 在 create_app 中预存（使 lifespan 优先读注入的 settings，测试可使用独立临时 DB）。
  - 关键决策：测试 fixture 使用 `app.router.lifespan_context(app)` 显式触发 ASGI lifespan，确保 `app.state.gpu_repo` 在请求前就绪（`httpx.ASGITransport` 本身不触发 lifespan）。这是 starlette/fastapi 集成测试的标准做法，无需引入 `asgi-lifespan` 外部依赖。

### [TASK-010] Azure/GPU 专用表 schema
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_gpu_schema.py` 27 个用例，全 passed 0 failed；`ruff check src/ tests/` 全绿。覆盖：migrate 幂等（五张表 + 5 个索引均存在）、ARCH-001 三通用表不受影响、servers CRUD（insert/get/get_all/delete、重复 name IntegrityError、不存在 id 返回 None/False）、级联删除（删 server → azure_vm_status 和 gpu_metrics 关联行自动清除）、azure_vm_status upsert 及 get_all、gpu_metrics append/get_latest（每卡最新行）/get_history（区间+limit+gpu_index 过滤）、降采样表接受插入 + UNIQUE INDEX 约束生效。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/db/schema.sql`：在 ARCH-001 三通用表之后追加 `-- === ARCH-002: Azure/GPU Tables ===` 分区，包含五张专用表完整 DDL（servers / azure_vm_status / gpu_metrics / gpu_metrics_5m / gpu_metrics_1h）及全部索引，全带 IF NOT EXISTS。migrate.py 无需改动（executescript 幂等执行 schema.sql 全部语句）。
  - `src/panel/db/gpu_repository.py`（新建）：GpuRepository 封装五张专用表读写；内含 GpuSample（采集器内部传输对象，TASK-013 使用）和 ServerRow / VmStatusRow / GpuMetricRow 行类型（slots dataclass）。append_gpu_metrics 自动计算 mem_pct；get_latest_gpu_metrics 用子查询取每卡最大 id；get_gpu_history 内层 DESC+LIMIT 取最近 N 条再外层 ASC，与通用 repository 策略一致。insert_server 返回新 id；delete_server 依赖 ON DELETE CASCADE 自动清理关联行。凭证字段 ssh_key_path 在 ServerRow 保留（供采集器读取路径），API 层 Pydantic response model（ServerOut）将其排除，不回传前端。
  - `src/panel/domain/models.py`：补充 ARCH-002 全部 Pydantic 模型：ServerIn（写入体，含 ssh_key_path）、ServerOut（响应体，故意不含 ssh_key_path）、VmStatusOut、GpuMetricOut、CollectorStatusOut、DashboardVmOut、DashboardAzureOut；ServerOut 继承 PublicModel（extra="forbid"，from_attributes=True）。
  - 关键决策：GpuSample 定义在 gpu_repository.py（与行类型同文件，TASK-013 直接 from panel.db.gpu_repository import GpuSample），避免循环导入；mem_pct 在 append 层计算而非在采集层强制，让采集器保持简单（无需关心比例计算）；降采样表 TASK-010 只建表不填充，填充逻辑留 TASK-016。

### [MS-001] 集成冒烟与修复
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 用仓库根 `.venv` 跑全套 `pytest tests/`,**69 passed 0 failed**;`ruff check src/` 全绿。真实 uvicorn(`panel.main:app`,127.0.0.1:8080)启动验证:`GET /healthz` → HTTP 200 且 `{"status":"ok","db":"ok",...}`;`GET /`(iPhone UA)→ 200 含 `panel-grid` 栅格容器、加载 `panel.js`、无 meta refresh;`GET /`(Kindle UA)→ 200 含 `panel-grid`、含 `<meta http-equiv="refresh" content="60">`、不加载 `panel.js`(无 JS 降级有效);`/static/css/panel.css` → 200。冒烟 DB `PRAGMA journal_mode=wal`,三表(latest_snapshot/metric_history/collector_run)建齐。空 Collector(`NullCollector` 返回 `[]`)注册→调度→`collector_run.status=up` 落库由 test_collectors 覆盖。验证完毕关闭进程,lifespan 日志显示 startup/shutdown 完整无异常。
- **备注**: 执行模型 claude-opus-4-8[1m]。MS-001 验收达成:app 启动 / healthz=200 / 空面板三端渲染骨架(栅格容器 + Kindle meta-refresh 降级)/ 采集框架可注册空 collector。
  - 集成修复 1:`main.lifespan` 此前未把 `Settings` 挂到 `app.state`,而 `web/routes.py` 通过 `getattr(app.state, "settings", None)` 读 `stale_threshold_seconds`,导致配置的 `PANEL_STALE_THRESHOLD_SECONDS` 被忽略、永远回落默认 180。已在 lifespan DB 初始化前补 `app.state.settings = settings`,使 SSR 路由按配置计算 stale 展示态。
  - 集成修复 2:`main.main()` 启动入口硬编码 `port=8080`,未读配置。改为 `port=get_settings().port`,与 `PANEL_PORT`/Settings.port 对齐(默认仍 8080,与 ARCH-001/.env.example 一致)。
  - 装配核查结论:`create_app`/`lifespan` 装配完整且正确——lifespan 内 DB 连接(WAL)→ 幂等建表 → Repository → register_collectors → build_scheduler → scheduler.start(),finally 先 scheduler.shutdown(wait=False) 再 conn.close();create_app 内 mount /static + include health/web 路由。无 import 错误、无表缺失、scheduler 正常启停。除上述两处最小修复外未改动任何功能逻辑。

### [TASK-004] SSR 前端壳:base 布局 + 响应式/e-ink CSS + 轮询/meta-refresh 降级
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_web.py` 8 个用例:GET / 返回 200 含 panel-grid 容器与 viewport meta;?eink=1 含 meta refresh 且不加载 panel.js;Kindle UA 含 meta refresh 且不加载 panel.js;默认请求无 meta refresh 且加载 panel.js;数据源状态条渲染 up/down/error/stale 四态(含 ●○◌ 形状符号与在线/离线/异常/数据陈旧文字标签);SSR 首屏无需 JS 即含完整 collector 名称;无 repo 时优雅降级 200;CSS 三重编码正确且无 box-shadow/animation(e-ink 约束)。全套 69 passed 0 failed,`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/web/routes.py`:用 Jinja2Templates,GET / 容错读 `app.state.repo`(不存在则空列表);stale 判定在路由层算(ran_at 距 now > stale_threshold_seconds → display_status="stale");`_is_eink()` 检查 ?eink=1 或 UA 含 Kindle/Silk。Starlette ≥1.x 新签名 `TemplateResponse(request=, name=, context=)`。
  - `src/panel/web/templates/base.html`:含 viewport meta;`{% if is_eink %}` 条件输出 `<meta http-equiv="refresh" content="60">`;非 e-ink 时加载 `/static/js/panel.js`;`<main class="panel-grid" id="panel-grid" data-poll-interval="45">` 为权威栅格容器。
  - `src/panel/web/templates/index.html`:extends base,include `partials/_datasource_status.html`,预留模块卡 include 插槽(注释说明模块卡约定 `<section class="card" data-module="...">`)。
  - `src/panel/web/templates/partials/_datasource_status.html`:按四态渲染,形状符号(●/○/◌)+ 文字标签(在线/离线/异常/数据陈旧)+ CSS class 三重编码;`any_issues` 时显示 `.datasource-banner--issues` 警示。
  - `src/panel/web/static/css/panel.css`:CSS token 系统(--bg/#0f1117 等 6 色);三断点栅格(<600px 单列/600–1024px 双列/>1024px auto-fill);`.card/.status-dot/.status-pill/.metric-bar/.datasource-banner` 等可复用类;e-ink 硬约束已强制(无 box-shadow、无 animation、无 @keyframes、print 媒体高对比)。
  - `src/panel/web/static/js/panel.js`:Page Visibility API 暂停/恢复轮询(不可见时零请求);fetch('/') 替换 #panel-grid innerHTML;活时钟每秒更新 #site-time;e-ink 设备服务端不输出此 script。
  - `src/panel/main.py`:接入静态资源挂载(`/static` → StaticFiles)与 `web_routes.router`。

### [TASK-003] Collector 框架:协议 + 注册表 + APScheduler 调度 + 框架级降级
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_collectors.py` 12 个用例:registry 注册/重复抛 ValueError/iter/clear、Collector runtime_checkable;run_collector 正常路径(写 snapshot+history、collector_run.status=up、sample_count 正确、get_last_success 非空)、异常路径(collect 抛 → error、无 snapshot/history、函数不外泄)、超时路径(collect 超 timeout → down、error="timeout"、不外泄)、error 脱敏(token=... 被打码)、失败隔离(并发跑一好一坏,好的完整落库、坏的不污染);build_scheduler 每 collector 一个 job 且 id==name、返回未 start;lifespan 集成(注册 collector→经 app.router.lifespan_context 启动→首采 status=up 落库→退出后 scheduler.running=False)。全套 61 passed 0 failed,`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - `src/panel/collectors/registry.py`:进程级 dict 注册表;`register` 重复 name 抛 ValueError,`get/iter_collectors/clear` 按契约(iter 按注册顺序,clear 供测试)。
  - `src/panel/collectors/scheduler.py`:`run_collector(collector, repo)` 严格按 ARCH-001 降级表实现——`asyncio.timeout(timeout_seconds)` 包 collect();成功→upsert_snapshot+append_history+record_collector_run(up);TimeoutError→down(error="timeout");其它 Exception→error(error=`scrub(str(exc))`);写库阶段异常亦降级为 error。`asyncio.CancelledError` 不吞、向上重抛(让 scheduler shutdown 正常取消任务)。`_record` 包 record_collector_run,即便落库失败也只 log 不外泄,保证 collector_run 必达且异常永不外泄。duration_ms 用 `time.monotonic()` 差值。`build_scheduler(repo)` 读 registry,每 collector add_job(IntervalTrigger(seconds=interval_seconds), id=name, max_instances=1, coalesce=True, next_run_time=now 触发首采, replace_existing=True),返回未 start 的 AsyncIOScheduler。
  - `src/panel/collectors/__init__.py`:提供 `register_collectors(settings, repo)` 集中注册入口(本卡空实现 + docstring 写明模块卡按「配置存在则注册、缺失则 warning 跳过」契约接入各模块工厂)。
  - 接入:`main.lifespan` 在 DB 初始化之后接 `register_collectors → build_scheduler → scheduler.start() → app.state.scheduler`;finally 先 `scheduler.shutdown(wait=False)` 再 `await conn.close()`(顺序遵 ARCH-001)。
  - 关键决策:TASK-002 已先行落地 `collectors/base.py` 的数据类型,本卡只在其上构建 registry/scheduler,未改 base.py 字段。`run_collector` 用「分支显式 return + _record 兜底」而非单一 try/finally,以便对超时/异常/写库失败三态分别构造 CollectorResult 且 record 必达。NullCollector/RaisingCollector/SlowCollector 仅测试夹具,不在生产注册;registry 用 autouse fixture 每例前后 clear 防污染。

### [TASK-002] SQLite(WAL) 连接 + 通用 schema 基线 + repository 薄层
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_db.py` 12 个用例:WAL journal_mode/foreign_keys 生效、migrate 幂等+三表存在、upsert(insert→update 不增行+空列表 noop)、get_snapshot/get_snapshot_metric、append_history 每次增行、get_history(区间过滤+limit 截最近 N 仍升序+按 target/metric 过滤)、record_collector_run + get_last_success(有/无 up 行)、get_all_last_runs 每 collector 最近一行、/healthz 经 lifespan 起 DB 后 `db="ok"`。全套 49 passed 0 failed,`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - `src/panel/db/connection.py`:`connect(db_path)` 设 WAL/synchronous=NORMAL/foreign_keys=ON/busy_timeout=5000,row_factory=aiosqlite.Row,文件型路径自动建父目录(:memory:/file: 跳过)。
  - `src/panel/db/schema.sql`:逐字采用 ARCH-001 三表 DDL(latest_snapshot/metric_history/collector_run,全 IF NOT EXISTS + 三索引)。`migrate.py` 读同目录 schema.sql(`Path(__file__).parent`)经 `executescript` + commit,幂等。
  - `src/panel/db/repository.py`:实现 SnapshotRow/HistoryRow/CollectorRunRow(slots dataclass)与 Repository 全部契约方法。upsert 用 `ON CONFLICT(collector,target_id,metric) DO UPDATE`(executemany,单次 commit);时间统一 `_iso()` 归一化为 ISO8601 UTC 存库、`_parse_utc()` 还原带 tz datetime;get_history 内层 DESC+LIMIT 取最近 N 再外层 ASC 呈现;get_all_last_runs 用 `MAX(id)` 自连接取每 collector 最新行;get_last_success 取最新 status='up' 的 ran_at。
  - 接入:`main.lifespan` 内 `connect → migrate.run → app.state.db/repo`,finally `await conn.close()`(scheduler 仍留 TASK-003 占位)。`api/health.py` 改为对 `app.state.db` 执行 `SELECT 1`,无连接或异常置 `db="down"`,整体恒 200,三字段 schema 不变。
  - 关键决策:repository 写方法签名依赖 `MetricSample`/`CollectorResult`,而其定义属 `collectors/base.py`(归 TASK-003)。由于 TASK-003 依赖 TASK-002,故本卡先落地 `collectors/base.py` 的数据类型(逐字采用 ARCH-001:MetricSample/CollectorResult/Collector 协议),TASK-003 在其上构建 registry/scheduler 即可,勿改字段。测试用临时文件 DB(WAL 需文件,不用 :memory:);healthz lifespan 测试用 `app.router.lifespan_context` 手动驱动(ASGITransport 不自动触发 startup/shutdown)。

### [TASK-005] 配置与凭证管理 + response model 白名单 + 日志脱敏
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: `tests/test_config.py` 33 个用例(Settings/get_settings/read_secret/scrub/setup_logging/PublicModel 全覆盖),加原有 `tests/test_health.py` 2 个用例;合计 35 passed 0 failed。`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/config/settings.py`:实现 `Settings(BaseSettings)`,env_prefix=`PANEL_`,env_file=`.env`,extra=`ignore`。字段含 db_path/port/log_level/stale_threshold_seconds/secrets_dir/azure_tenant_id/azure_client_id/azure_client_secret_file/tailscale_socket/ssh_key_path;凭证字段仅接受路径引用(不存明文)。`read_secret(name_or_path, settings=None)` helper 支持 secrets_dir 相对名称和绝对路径两种形式,均 strip 空白。`get_settings()` 以 `@lru_cache` 实现单例。
  - `src/panel/config/scrub.py`:正则集合覆盖 token/secret/password/api_key/key kv 模式、Bearer/Basic 授权头、PEM 私钥块、长 hex/base64 串。`scrub(text)` 全 pass 安全(空串原样返回)。`_ScrubFilter` 先调 `record.getMessage()` 完全插值再脱敏后置 `record.args=None`,避免 `%s` 占位符计数失配 TypeError。`setup_logging(level)` 配置 root logger + 安装 scrub filter + 静默 uvicorn.access/apscheduler,幂等可重复调用。
  - `src/panel/domain/models.py`:实现 `PublicModel(BaseModel)` 白名单基类,`ConfigDict(extra="forbid")`。文档化禁用字段名模式 (*secret*/*token*/*key*/*password*/ssh_key_path/private_*)。
  - `src/panel/main.py`:接入 `Settings`、`get_settings()`、`setup_logging()`。`create_app(settings)` 内优先使用传入 settings,否则调 `get_settings()`;调用 `setup_logging(resolved.log_level)` 完成日志初始化。移除旧的 `TYPE_CHECKING` 条件导入占位。
  - `.env.example`:补充 `PANEL_STALE_THRESHOLD_SECONDS`/`PANEL_SECRETS_DIR`/凭证路径约定说明。
  - `docker-compose.yml`:补充 `./secrets:/secrets:ro` 挂载约定注释与说明。
  - 关键决策:`scrub` 采用「先插值再脱敏」策略解决日志 filter 中 `%` 格式化与脱敏改变占位符数量的冲突;凭证字段统一只存路径、不存明文,由 `read_secret()` 在运行时读取。

### [TASK-001] 项目骨架 + Dockerfile + compose + /healthz(声明全量依赖)
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: `tests/test_health.py` 2 个用例(httpx.ASGITransport 打 `/healthz` 断言 200 与 `status/db/time` schema + tz-aware time;`create_app` 工厂独立实例)。`ruff check src tests` 通过,`pytest` 2 passed。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - 按 ARCH-001 分层创建完整骨架 `src/panel/{config,db,collectors,api,web,domain}/`(含 `__init__.py` 与 web 模板/静态目录)与 `tests/`。
  - `pyproject.toml`(hatchling, src 布局)一次性声明全量依赖:运行时 14(fastapi/uvicorn[standard]/uvloop/jinja2/aiosqlite/apscheduler/httpx/aiohttp/asyncssh/azure-identity/azure-mgmt-compute/pydantic/pydantic-settings/python-multipart)+ 开发 4(pytest/pytest-asyncio/pytest-cov/ruff);ruff(line-length 100, py312)与 pytest(`asyncio_mode=auto`)已配。后续模块卡不再改 pyproject。
  - `main.py`:`create_app(settings=None) -> FastAPI` 工厂 + `lifespan` asynccontextmanager,留好 TASK-002(DB conn/repo→app.state)/TASK-003(register_collectors+build_scheduler.start/shutdown)/TASK-004(StaticFiles mount + web router)的挂载点(均以 TODO 标注,函数签名稳定)。模块级 `app = create_app()` 供 `uvicorn panel.main:app`;`main()` 入口用 `loop="uvloop"` 单 worker。
  - `api/health.py`:`GET /healthz` → 200 `{"status","db","time(iso8601 UTC)}`;`db` 本卡固定 "ok" 占位,TASK-002 接入后改真实 `SELECT 1`,schema 不变。
  - 容器化:多阶段 Dockerfile(python:3.12-slim-bookworm;builder `--prefix=/install` → runtime 整体拷贝;非 root `app` uid 10001;HEALTHCHECK 命中 /healthz)。compose 用 v2 顶层 `mem_limit: 512m` + `restart: unless-stopped` + `./data:/data` 卷 + healthcheck;Tailscale socket / secrets 卷以注释预留。`.env.example`/`.dockerignore`/`.gitignore` 齐备。
  - 环境约束:宿主默认 python 为 3.14,改用 `/usr/bin/python3.12` 建 `.venv`;系统缺 `python3.12-venv`(无 ensurepip),用 `--without-pip` 建 venv 后 get-pip.py 引导 pip,再 `pip install -e ".[dev]"` 全装成功。
  - 验证:`.venv` 内 uvicorn(uvloop)真实端口起服务 /healthz 返回 200;`docker build` amd64 成功,容器 `whoami=app`(非 root)、/healthz 200、HEALTHCHECK 变 healthy;多 arch(linux/arm64,linux/amd64)buildx 构建在 docker-container builder + qemu-aarch64 下进行。
