# 2026-06

### [TASK-011] 服务器注册 CRUD API（凭证不回传）
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_servers_api.py` 20 个用例，全 passed 0 failed；全套回归 116 passed 0 failed；`ruff check` 全绿。覆盖：POST 成功 201 / 响应字段集合与 ServerOut 精确一致 / 响应体不含 ssh_key_path / 响应值校验 / 重复 name 409 / 最小 payload 成功 / 缺少 name 422；GET 空库返回空数组 / 注册后列表出现 / 列表无 ssh_key_path / 多台有序返回 / 列表字段集合一致；DELETE 存在 id 204 / 不存在 id 404 / DELETE 后 GET 消失 / 只删目标不影响其他 / 两次 DELETE 第二次 404；DB 直接 SQL 验证 ssh_key_path 确实写入；/healthz 与 /api/v1/servers 同进程可访问。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/api/deps.py`（新建）：`get_repo` 和 `get_gpu_repo` 两个依赖注入函数，从 `request.app.state` 读取对应 Repository 实例，与 ARCH-001 deps.py 模式对齐。
  - `src/panel/api/azure.py`（新建）：`APIRouter(prefix="/api/v1", tags=["servers"])`，三个端点 POST/GET/DELETE `/servers`；`_row_to_out` 显式字段映射（不用 `**dict`），彻底隔离 ssh_key_path；POST 捕获 `aiosqlite.IntegrityError` 转 409（raise from exc），通用异常转 500（仅记录异常类型，不记录路径内容）；DELETE 根据 `delete_server()` 布尔返回值决定 204 或 404。
  - `src/panel/main.py`（修改）：lifespan 中追加 `app.state.gpu_repo = GpuRepository(conn)`；create_app 中 `app.include_router(azure_router)`；同时修复 `app.state.settings` 在 create_app 中预存（使 lifespan 优先读注入的 settings，测试可使用独立临时 DB）。
  - 关键决策：测试 fixture 使用 `app.router.lifespan_context(app)` 显式触发 ASGI lifespan，确保 `app.state.gpu_repo` 在请求前就绪（`httpx.ASGITransport` 本身不触发 lifespan）。这是 starlette/fastapi 集成测试的标准做法，无需引入 `asgi-lifespan` 外部依赖。

### [TASK-010] Azure/GPU 专用表 schema
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_gpu_schema.py` 27 个用例，全 passed 0 failed；`ruff check src/ tests/` 全绿。覆盖：migrate 幂等（五张表 + 5 个索引均存在）、ARCH-001 三通用表不受影响、servers CRUD（insert/get/get_all/delete、重复 name IntegrityError、不存在 id 返回 None/False）、级联删除（删 server → azure_vm_status 和 gpu_metrics 关联行自动清除）、azure_vm_status upsert 及 get_all、gpu_metrics append/get_latest（每卡最新行）/get_history（区间+limit+gpu_index 过滤）、降采样表接受插入 + UNIQUE INDEX 约束生效。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/db/schema.sql`：在 ARCH-001 三通用表之后追加 `-- === ARCH-002: Azure/GPU Tables ===` 分区，包含五张专用表完整 DDL（servers / azure_vm_status / gpu_metrics / gpu_metrics_5m / gpu_metrics_1h）及全部索引，全带 IF NOT EXISTS。migrate.py 无需改动（executescript 幂等执行 schema.sql 全部语句）。
  - `src/panel/db/gpu_repository.py`（新建）：GpuRepository 封装五张专用表读写；内含 GpuSample（采集器内部传输对象，TASK-013 使用）和 ServerRow / VmStatusRow / GpuMetricRow 行类型（slots dataclass）。append_gpu_metrics 自动计算 mem_pct；get_latest_gpu_metrics 用子查询取每卡最大 id；get_gpu_history 内层 DESC+LIMIT 取最近 N 条再外层 ASC，与通用 repository 策略一致。insert_server 返回新 id；delete_server 依赖 ON DELETE CASCADE 自动清理关联行。凭证字段 ssh_key_path 在 ServerRow 保留（供采集器读取路径），API 层 Pydantic response model（ServerOut）将其排除，不回传前端。
  - `src/panel/domain/models.py`：补充 ARCH-002 全部 Pydantic 模型：ServerIn（写入体，含 ssh_key_path）、ServerOut（响应体，故意不含 ssh_key_path）、VmStatusOut、GpuMetricOut、CollectorStatusOut、DashboardVmOut、DashboardAzureOut；ServerOut 继承 PublicModel（extra="forbid"，from_attributes=True）。
  - 关键决策：GpuSample 定义在 gpu_repository.py（与行类型同文件，TASK-013 直接 from panel.db.gpu_repository import GpuSample），避免循环导入；mem_pct 在 append 层计算而非在采集层强制，让采集器保持简单（无需关心比例计算）；降采样表 TASK-010 只建表不填充，填充逻辑留 TASK-016。

### [MS-001] 集成冒烟与修复
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 用仓库根 `.venv` 跑全套 `pytest tests/`,**69 passed 0 failed**;`ruff check src/` 全绿。真实 uvicorn(`panel.main:app`,127.0.0.1:8080)启动验证:`GET /healthz` → HTTP 200 且 `{"status":"ok","db":"ok",...}`;`GET /`(iPhone UA)→ 200 含 `panel-grid` 栅格容器、加载 `panel.js`、无 meta refresh;`GET /`(Kindle UA)→ 200 含 `panel-grid`、含 `<meta http-equiv="refresh" content="60">`、不加载 `panel.js`(无 JS 降级有效);`/static/css/panel.css` → 200。冒烟 DB `PRAGMA journal_mode=wal`,三表(latest_snapshot/metric_history/collector_run)建齐。空 Collector(`NullCollector` 返回 `[]`)注册→调度→`collector_run.status=up` 落库由 test_collectors 覆盖。验证完毕关闭进程,lifespan 日志显示 startup/shutdown 完整无异常。
- **备注**: 执行模型 claude-opus-4-8[1m]。MS-001 验收达成:app 启动 / healthz=200 / 空面板三端渲染骨架(栅格容器 + Kindle meta-refresh 降级)/ 采集框架可注册空 collector。
  - 集成修复 1:`main.lifespan` 此前未把 `Settings` 挂到 `app.state`,而 `web/routes.py` 通过 `getattr(app.state, "settings", None)` 读 `stale_threshold_seconds`,导致配置的 `PANEL_STALE_THRESHOLD_SECONDS` 被忽略、永远回落默认 180。已在 lifespan DB 初始化前补 `app.state.settings = settings`,使 SSR 路由按配置计算 stale 展示态。
  - 集成修复 2:`main.main()` 启动入口硬编码 `port=8080`,未读配置。改为 `port=get_settings().port`,与 `PANEL_PORT`/Settings.port 对齐(默认仍 8080,与 ARCH-001/.env.example 一致)。
  - 装配核查结论:`create_app`/`lifespan` 装配完整且正确——lifespan 内 DB 连接(WAL)→ 幂等建表 → Repository → register_collectors → build_scheduler → scheduler.start(),finally 先 scheduler.shutdown(wait=False) 再 conn.close();create_app 内 mount /static + include health/web 路由。无 import 错误、无表缺失、scheduler 正常启停。除上述两处最小修复外未改动任何功能逻辑。

### [TASK-004] SSR 前端壳:base 布局 + 响应式/e-ink CSS + 轮询/meta-refresh 降级
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_web.py` 8 个用例:GET / 返回 200 含 panel-grid 容器与 viewport meta;?eink=1 含 meta refresh 且不加载 panel.js;Kindle UA 含 meta refresh 且不加载 panel.js;默认请求无 meta refresh 且加载 panel.js;数据源状态条渲染 up/down/error/stale 四态(含 ●○◌ 形状符号与在线/离线/异常/数据陈旧文字标签);SSR 首屏无需 JS 即含完整 collector 名称;无 repo 时优雅降级 200;CSS 三重编码正确且无 box-shadow/animation(e-ink 约束)。全套 69 passed 0 failed,`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/web/routes.py`:用 Jinja2Templates,GET / 容错读 `app.state.repo`(不存在则空列表);stale 判定在路由层算(ran_at 距 now > stale_threshold_seconds → display_status="stale");`_is_eink()` 检查 ?eink=1 或 UA 含 Kindle/Silk。Starlette ≥1.x 新签名 `TemplateResponse(request=, name=, context=)`。
  - `src/panel/web/templates/base.html`:含 viewport meta;`{% if is_eink %}` 条件输出 `<meta http-equiv="refresh" content="60">`;非 e-ink 时加载 `/static/js/panel.js`;`<main class="panel-grid" id="panel-grid" data-poll-interval="45">` 为权威栅格容器。
  - `src/panel/web/templates/index.html`:extends base,include `partials/_datasource_status.html`,预留模块卡 include 插槽(注释说明模块卡约定 `<section class="card" data-module="...">`)。
  - `src/panel/web/templates/partials/_datasource_status.html`:按四态渲染,形状符号(●/○/◌)+ 文字标签(在线/离线/异常/数据陈旧)+ CSS class 三重编码;`any_issues` 时显示 `.datasource-banner--issues` 警示。
  - `src/panel/web/static/css/panel.css`:CSS token 系统(--bg/#0f1117 等 6 色);三断点栅格(<600px 单列/600–1024px 双列/>1024px auto-fill);`.card/.status-dot/.status-pill/.metric-bar/.datasource-banner` 等可复用类;e-ink 硬约束已强制(无 box-shadow、无 animation、无 @keyframes、print 媒体高对比)。
  - `src/panel/web/static/js/panel.js`:Page Visibility API 暂停/恢复轮询(不可见时零请求);fetch('/') 替换 #panel-grid innerHTML;活时钟每秒更新 #site-time;e-ink 设备服务端不输出此 script。
  - `src/panel/main.py`:接入静态资源挂载(`/static` → StaticFiles)与 `web_routes.router`。

### [TASK-003] Collector 框架:协议 + 注册表 + APScheduler 调度 + 框架级降级
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_collectors.py` 12 个用例:registry 注册/重复抛 ValueError/iter/clear、Collector runtime_checkable;run_collector 正常路径(写 snapshot+history、collector_run.status=up、sample_count 正确、get_last_success 非空)、异常路径(collect 抛 → error、无 snapshot/history、函数不外泄)、超时路径(collect 超 timeout → down、error="timeout"、不外泄)、error 脱敏(token=... 被打码)、失败隔离(并发跑一好一坏,好的完整落库、坏的不污染);build_scheduler 每 collector 一个 job 且 id==name、返回未 start;lifespan 集成(注册 collector→经 app.router.lifespan_context 启动→首采 status=up 落库→退出后 scheduler.running=False)。全套 61 passed 0 failed,`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - `src/panel/collectors/registry.py`:进程级 dict 注册表;`register` 重复 name 抛 ValueError,`get/iter_collectors/clear` 按契约(iter 按注册顺序,clear 供测试)。
  - `src/panel/collectors/scheduler.py`:`run_collector(collector, repo)` 严格按 ARCH-001 降级表实现——`asyncio.timeout(timeout_seconds)` 包 collect();成功→upsert_snapshot+append_history+record_collector_run(up);TimeoutError→down(error="timeout");其它 Exception→error(error=`scrub(str(exc))`);写库阶段异常亦降级为 error。`asyncio.CancelledError` 不吞、向上重抛(让 scheduler shutdown 正常取消任务)。`_record` 包 record_collector_run,即便落库失败也只 log 不外泄,保证 collector_run 必达且异常永不外泄。duration_ms 用 `time.monotonic()` 差值。`build_scheduler(repo)` 读 registry,每 collector add_job(IntervalTrigger(seconds=interval_seconds), id=name, max_instances=1, coalesce=True, next_run_time=now 触发首采, replace_existing=True),返回未 start 的 AsyncIOScheduler。
  - `src/panel/collectors/__init__.py`:提供 `register_collectors(settings, repo)` 集中注册入口(本卡空实现 + docstring 写明模块卡按「配置存在则注册、缺失则 warning 跳过」契约接入各模块工厂)。
  - 接入:`main.lifespan` 在 DB 初始化之后接 `register_collectors → build_scheduler → scheduler.start() → app.state.scheduler`;finally 先 `scheduler.shutdown(wait=False)` 再 `await conn.close()`(顺序遵 ARCH-001)。
  - 关键决策:TASK-002 已先行落地 `collectors/base.py` 的数据类型,本卡只在其上构建 registry/scheduler,未改 base.py 字段。`run_collector` 用「分支显式 return + _record 兜底」而非单一 try/finally,以便对超时/异常/写库失败三态分别构造 CollectorResult 且 record 必达。NullCollector/RaisingCollector/SlowCollector 仅测试夹具,不在生产注册;registry 用 autouse fixture 每例前后 clear 防污染。

### [TASK-002] SQLite(WAL) 连接 + 通用 schema 基线 + repository 薄层
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: 新增 `tests/test_db.py` 12 个用例:WAL journal_mode/foreign_keys 生效、migrate 幂等+三表存在、upsert(insert→update 不增行+空列表 noop)、get_snapshot/get_snapshot_metric、append_history 每次增行、get_history(区间过滤+limit 截最近 N 仍升序+按 target/metric 过滤)、record_collector_run + get_last_success(有/无 up 行)、get_all_last_runs 每 collector 最近一行、/healthz 经 lifespan 起 DB 后 `db="ok"`。全套 49 passed 0 failed,`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - `src/panel/db/connection.py`:`connect(db_path)` 设 WAL/synchronous=NORMAL/foreign_keys=ON/busy_timeout=5000,row_factory=aiosqlite.Row,文件型路径自动建父目录(:memory:/file: 跳过)。
  - `src/panel/db/schema.sql`:逐字采用 ARCH-001 三表 DDL(latest_snapshot/metric_history/collector_run,全 IF NOT EXISTS + 三索引)。`migrate.py` 读同目录 schema.sql(`Path(__file__).parent`)经 `executescript` + commit,幂等。
  - `src/panel/db/repository.py`:实现 SnapshotRow/HistoryRow/CollectorRunRow(slots dataclass)与 Repository 全部契约方法。upsert 用 `ON CONFLICT(collector,target_id,metric) DO UPDATE`(executemany,单次 commit);时间统一 `_iso()` 归一化为 ISO8601 UTC 存库、`_parse_utc()` 还原带 tz datetime;get_history 内层 DESC+LIMIT 取最近 N 再外层 ASC 呈现;get_all_last_runs 用 `MAX(id)` 自连接取每 collector 最新行;get_last_success 取最新 status='up' 的 ran_at。
  - 接入:`main.lifespan` 内 `connect → migrate.run → app.state.db/repo`,finally `await conn.close()`(scheduler 仍留 TASK-003 占位)。`api/health.py` 改为对 `app.state.db` 执行 `SELECT 1`,无连接或异常置 `db="down"`,整体恒 200,三字段 schema 不变。
  - 关键决策:repository 写方法签名依赖 `MetricSample`/`CollectorResult`,而其定义属 `collectors/base.py`(归 TASK-003)。由于 TASK-003 依赖 TASK-002,故本卡先落地 `collectors/base.py` 的数据类型(逐字采用 ARCH-001:MetricSample/CollectorResult/Collector 协议),TASK-003 在其上构建 registry/scheduler 即可,勿改字段。测试用临时文件 DB(WAL 需文件,不用 :memory:);healthz lifespan 测试用 `app.router.lifespan_context` 手动驱动(ASGITransport 不自动触发 startup/shutdown)。

### [TASK-005] 配置与凭证管理 + response model 白名单 + 日志脱敏
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: `tests/test_config.py` 33 个用例(Settings/get_settings/read_secret/scrub/setup_logging/PublicModel 全覆盖),加原有 `tests/test_health.py` 2 个用例;合计 35 passed 0 failed。`ruff check src tests` 全绿。
- **备注**: 执行模型 claude-sonnet-4-6。
  - `src/panel/config/settings.py`:实现 `Settings(BaseSettings)`,env_prefix=`PANEL_`,env_file=`.env`,extra=`ignore`。字段含 db_path/port/log_level/stale_threshold_seconds/secrets_dir/azure_tenant_id/azure_client_id/azure_client_secret_file/tailscale_socket/ssh_key_path;凭证字段仅接受路径引用(不存明文)。`read_secret(name_or_path, settings=None)` helper 支持 secrets_dir 相对名称和绝对路径两种形式,均 strip 空白。`get_settings()` 以 `@lru_cache` 实现单例。
  - `src/panel/config/scrub.py`:正则集合覆盖 token/secret/password/api_key/key kv 模式、Bearer/Basic 授权头、PEM 私钥块、长 hex/base64 串。`scrub(text)` 全 pass 安全(空串原样返回)。`_ScrubFilter` 先调 `record.getMessage()` 完全插值再脱敏后置 `record.args=None`,避免 `%s` 占位符计数失配 TypeError。`setup_logging(level)` 配置 root logger + 安装 scrub filter + 静默 uvicorn.access/apscheduler,幂等可重复调用。
  - `src/panel/domain/models.py`:实现 `PublicModel(BaseModel)` 白名单基类,`ConfigDict(extra="forbid")`。文档化禁用字段名模式 (*secret*/*token*/*key*/*password*/ssh_key_path/private_*)。
  - `src/panel/main.py`:接入 `Settings`、`get_settings()`、`setup_logging()`。`create_app(settings)` 内优先使用传入 settings,否则调 `get_settings()`;调用 `setup_logging(resolved.log_level)` 完成日志初始化。移除旧的 `TYPE_CHECKING` 条件导入占位。
  - `.env.example`:补充 `PANEL_STALE_THRESHOLD_SECONDS`/`PANEL_SECRETS_DIR`/凭证路径约定说明。
  - `docker-compose.yml`:补充 `./secrets:/secrets:ro` 挂载约定注释与说明。
  - 关键决策:`scrub` 采用「先插值再脱敏」策略解决日志 filter 中 `%` 格式化与脱敏改变占位符数量的冲突;凭证字段统一只存路径、不存明文,由 `read_secret()` 在运行时读取。

### [TASK-001] 项目骨架 + Dockerfile + compose + /healthz(声明全量依赖)
- **开始**: 2026-06-28
- **完成**: 2026-06-28
- **状态**: review
- **测试覆盖**: `tests/test_health.py` 2 个用例(httpx.ASGITransport 打 `/healthz` 断言 200 与 `status/db/time` schema + tz-aware time;`create_app` 工厂独立实例)。`ruff check src tests` 通过,`pytest` 2 passed。
- **备注**: 执行模型 claude-opus-4-8[1m]。
  - 按 ARCH-001 分层创建完整骨架 `src/panel/{config,db,collectors,api,web,domain}/`(含 `__init__.py` 与 web 模板/静态目录)与 `tests/`。
  - `pyproject.toml`(hatchling, src 布局)一次性声明全量依赖:运行时 14(fastapi/uvicorn[standard]/uvloop/jinja2/aiosqlite/apscheduler/httpx/aiohttp/asyncssh/azure-identity/azure-mgmt-compute/pydantic/pydantic-settings/python-multipart)+ 开发 4(pytest/pytest-asyncio/pytest-cov/ruff);ruff(line-length 100, py312)与 pytest(`asyncio_mode=auto`)已配。后续模块卡不再改 pyproject。
  - `main.py`:`create_app(settings=None) -> FastAPI` 工厂 + `lifespan` asynccontextmanager,留好 TASK-002(DB conn/repo→app.state)/TASK-003(register_collectors+build_scheduler.start/shutdown)/TASK-004(StaticFiles mount + web router)的挂载点(均以 TODO 标注,函数签名稳定)。模块级 `app = create_app()` 供 `uvicorn panel.main:app`;`main()` 入口用 `loop="uvloop"` 单 worker。
  - `api/health.py`:`GET /healthz` → 200 `{"status","db","time(iso8601 UTC)}`;`db` 本卡固定 "ok" 占位,TASK-002 接入后改真实 `SELECT 1`,schema 不变。
  - 容器化:多阶段 Dockerfile(python:3.12-slim-bookworm;builder `--prefix=/install` → runtime 整体拷贝;非 root `app` uid 10001;HEALTHCHECK 命中 /healthz)。compose 用 v2 顶层 `mem_limit: 512m` + `restart: unless-stopped` + `./data:/data` 卷 + healthcheck;Tailscale socket / secrets 卷以注释预留。`.env.example`/`.dockerignore`/`.gitignore` 齐备。
  - 环境约束:宿主默认 python 为 3.14,改用 `/usr/bin/python3.12` 建 `.venv`;系统缺 `python3.12-venv`(无 ensurepip),用 `--without-pip` 建 venv 后 get-pip.py 引导 pip,再 `pip install -e ".[dev]"` 全装成功。
  - 验证:`.venv` 内 uvicorn(uvloop)真实端口起服务 /healthz 返回 200;`docker build` amd64 成功,容器 `whoami=app`(非 root)、/healthz 200、HEALTHCHECK 变 healthy;多 arch(linux/arm64,linux/amd64)buildx 构建在 docker-container builder + qemu-aarch64 下进行。
